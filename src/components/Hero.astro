---
interface Props {
  h1?: string;
  sub?: string;
}

const brand  = import.meta.env.PUBLIC_BRAND   || '${brand}';
const aprMin = import.meta.env.PUBLIC_APRMIN  || '${aprMin}';
const cta    = import.meta.env.PUBLIC_CTA     || '${cta}';
const title2      = import.meta.env.PUBLIC_TITLE2       || '${title2}';
const badge = import.meta.env.PUBLIC_BADGE || 'Instant Approval';

const {
  h1  = import.meta.env.PUBLIC_H1  || '${h1}',
  sub = import.meta.env.PUBLIC_SUB || '${sub}',
} = Astro.props;

const trustItems = [
  { icon: `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>`, label: 'Soft credit check—zero impact on your score' },
  { icon: `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><path d="M9 12l2 2 4-4"/></svg>`, label: '0% APR plans for qualified pet owners' },
  { icon: `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M20.84 4.61a5.5 5.5 0 00-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 00-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 000-7.78z"/></svg>`, label: 'Trusted by 250,000+ pet parents nationwide' },
  { icon: `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>`, label: 'Complete transparency—no hidden fees' },
];
---

<!-- NAV -->
<header class="hero-nav fixed top-0 left-0 right-0 z-50">
  <div class="max-w-6xl mx-auto px-6 h-14 flex items-center justify-between">
    <div class="flex items-center gap-2.5">
      <div class="brand-icon-bg w-7 h-7 rounded-lg flex items-center justify-center">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="hsl(0 0% 100%)" stroke-width="2.5" stroke-linecap="round">
          <path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>
        </svg>
      </div>
      <span class="font-display font-bold text-foreground text-sm">{brand}</span>
    </div>

    <nav class="hidden md:flex items-center gap-7">
      <a href="#how-it-works" class="nav-link">How It Works</a>
      <a href="#calculator" class="nav-link">Calculator</a>
      <a href="#rates" class="nav-link">Rates</a>
      <a href="#faq" class="nav-link">FAQ</a>
    </nav>

    <a href="/?requested_amount=1500" class="btn-primary py-2.5 px-5 text-sm">{cta}</a>
  </div>
</header>

<!-- HERO -->
<section class="relative pt-14 px-6 overflow-hidden" style="background: hsl(var(--background));">
  <div class="max-w-6xl mx-auto w-full grid items-start gap-8 py-8 md:py-14 lg:grid-cols-[1.05fr_minmax(360px,420px)] lg:items-center">

    <!-- Copy — hero message. Mobile: appears AFTER the form (order-2). Desktop: left column. -->
    <div class="order-2 lg:order-1">
      <div class="flex items-center gap-2 mb-6">
        <span class="section-label !mb-0">Rates from {aprMin}% APR</span>
        <span class="badge">
          <span class="w-1.5 h-1.5 rounded-full bg-green-400 animate-pulse"></span>
          {badge}
        </span>
      </div>

      <h1 class="font-display font-bold leading-[1.08] tracking-tight text-3xl sm:text-4xl md:text-5xl lg:text-6xl text-foreground mb-5">
        {h1}<br/>
        <span class="gradient-text">{title2}</span>
      </h1>

      <p class="text-foreground/70 text-base md:text-lg leading-relaxed max-w-md">
        {sub}
      </p>

      <!-- Desktop-only decorative info cards -->
      <div class="relative h-[420px] mt-10 hidden lg:block" aria-hidden="true">
        <!-- Main card -->
        <div class="premium-card absolute top-8 left-0 right-8 p-6 animate-float" style="will-change: transform;">
          <div class="flex items-center justify-between mb-5">
            <div>
              <p class="text-foreground/40 text-xs font-medium uppercase tracking-widest mb-1">Financing Amount</p>
              <p class="font-display font-bold text-2xl text-foreground mono-num">$2,400</p>
            </div>
            <div class="brand-icon-bg w-10 h-10 rounded-xl flex items-center justify-center">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="hsl(0 0% 100%)" stroke-width="2.5" stroke-linecap="round">
                <path d="M12 2v20M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/>
              </svg>
            </div>
          </div>

          <!-- Progress bar -->
          <div class="mb-5">
            <div class="flex justify-between text-xs text-foreground/40 mb-2">
              <span>3 of 12 payments</span>
              <span class="text-primary font-semibold">On Schedule</span>
            </div>
            <div class="h-1.5 bg-border rounded-full overflow-hidden">
              <div class="hero-progress-bar h-full rounded-full w-[12%]"></div>
            </div>
          </div>

          <div class="grid grid-cols-3 gap-3">
            {[
              { label: 'Monthly', val: '$200' },
              { label: 'APR', val: '0%' },
              { label: 'Term', val: '12mo' },
            ].map(stat => (
              <div class="hero-stat-chip rounded-xl p-3 text-center">
                <div class="font-display font-bold text-foreground text-sm mono-num">{stat.val}</div>
                <div class="text-foreground/50 text-xs mt-0.5">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>

        <!-- Approval card -->
        <div class="glass-card absolute bottom-4 right-0 left-12 p-4 animate-float-slow flex items-center gap-4" style="will-change: transform;">
          <div class="w-10 h-10 rounded-xl flex items-center justify-center flex-shrink-0" style="background: hsl(var(--primary) / 0.12);">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="hsl(var(--primary))" stroke-width="2.5" stroke-linecap="round">
              <path d="M20 6L9 17l-5-5"/>
            </svg>
          </div>
          <div>
            <p class="text-foreground font-semibold text-sm">Pet Care Plan Approved</p>
            <p class="text-foreground/40 text-xs">Instant decision — zero credit impact</p>
          </div>
          <div class="ml-auto text-xs font-mono px-2 py-1 rounded-lg" style="color: hsl(var(--primary)); background: hsl(var(--primary) / 0.10);">+$2,400</div>
        </div>

        <!-- Rate badge -->
        <div class="glass-card absolute top-4 right-0 p-4 w-36 text-center animate-float hero-rate-badge" style="will-change: transform;">
          <div class="text-foreground/40 text-xs mb-1 uppercase tracking-wider">Your Rate</div>
          <div class="font-display font-bold text-2xl text-primary mono-num">0<span class="text-sm font-semibold">%</span></div>
          <div class="text-foreground/30 text-xs mt-1">APR Option</div>
        </div>
      </div>
    </div>

    <!-- Form — the real capture widget. Mobile: appears FIRST (order-1), directly under the header. -->
    <div class="order-1 lg:order-2">
      <div class="premium-card p-4 md:p-5">
        <div class="flex items-start justify-between gap-3 mb-3.5">
          <h2 class="font-display font-bold text-foreground text-lg leading-snug">Check your options</h2>
          <span class="inline-flex shrink-0 items-center gap-1 rounded-full px-2 py-1 text-[11px] font-semibold" style="background: hsl(var(--primary) / 0.1); color: hsl(var(--primary));">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>
            Secure
          </span>
        </div>

        <!-- Amount selection -->
        <div>
          <p class="text-foreground/70 text-xs font-semibold uppercase tracking-wider mb-2.5">Select your financing amount</p>
          <div class="grid grid-cols-3 gap-1.5" id="amount-grid">
            {[
              { label: '$500',   val: '500'   },
              { label: '$1,000', val: '1000'  },
              { label: '$1,500', val: '1500'  },
              { label: '$2,000', val: '2000'  },
              { label: '$3,000', val: '3000'  },
              { label: '$5,000', val: '5000'  },
            ].map((btn, i) => (
              <button
                type="button"
                data-amount={btn.val}
                class={`amount-btn h-11 rounded-[calc(var(--radius)-2px)] text-sm font-semibold duration-200 border focus:outline-none focus:ring-2 focus:ring-primary/30 ${i === 2 ? 'amount-btn--active' : ''}`}
              >
                {btn.label}
              </button>
            ))}
          </div>
        </div>

        <div class="relative flex items-center mt-3.5">
          <input
            id="zip-input"
            type="text"
            inputmode="numeric"
            maxlength="5"
            placeholder="Enter your ZIP code"
            autocomplete="postal-code"
            class="text-base py-4 px-5 pr-12 rounded-[var(--radius)] border border-border bg-surface text-foreground placeholder-foreground/30 focus:outline-none focus:border-primary/50 focus:ring-1 focus:ring-primary/30 w-full duration-200"
          />
          <svg class="absolute right-4 pointer-events-none text-foreground/30" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/>
          </svg>
        </div>
        <div id="zip-city-hint" class="zip-city-hint" aria-live="polite" aria-atomic="true"></div>
        <div class="relative mt-1">
          <button
            id="hero-cta"
            type="button"
            class="btn-primary text-base py-4 px-8 w-full justify-center"
          >
            {cta}
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </button>
          <p id="hero-cta-error" class="text-red-500 text-xs mt-2 hidden" aria-live="polite"></p>
        </div>

        <p class="text-foreground/55 text-[11.5px] leading-relaxed mt-3">
          Soft credit check only — checking your options never affects your credit score.
        </p>
      </div>

      <!-- Reassurance chips — directly under the form, before the hero message on mobile -->
      <ul class="mt-3 grid grid-cols-2 gap-2">
        {trustItems.map(item => (
          <li class="flex items-center gap-1.5 rounded-[10px] border border-border bg-surface px-2.5 py-2 text-[11.5px] font-medium text-foreground/70">
            <span class="text-primary shrink-0" set:html={item.icon} />
            {item.label}
          </li>
        ))}
      </ul>
    </div>
  </div>

  <script>
    var grid     = document.getElementById('amount-grid');
    var ctaBtn   = document.getElementById('hero-cta');
    var ctaError = document.getElementById('hero-cta-error');
    var zipInput = document.getElementById('zip-input');
    var cityHint = document.getElementById('zip-city-hint');

    var zipData = null;

    (window.requestIdleCallback || function(cb){ setTimeout(cb, 300); })(function() {
      fetch(import.meta.env.BASE_URL + 'zip-cities.json')
        .then(function(r) { return r.json(); })
        .then(function(d) { zipData = d; })
        .catch(function() {});
    });

    function isValidZip(zip) {
      return /^\d{5}$/.test(zip);
    }

    function getCityForZip(zip) {
      return zipData ? (zipData[zip] || null) : null;
    }

    function showError(msg) {
      if (!ctaError) return;
      ctaError.textContent = msg;
      ctaError.classList.remove('hidden');
    }

    function clearError() {
      if (!ctaError) return;
      ctaError.textContent = '';
      ctaError.classList.add('hidden');
    }

    function updateCityHint(zip) {
      if (!cityHint) return;
      if (zip.length === 5 && /^\d{5}$/.test(zip)) {
        var city = getCityForZip(zip);
        if (city) {
          cityHint.textContent = '\u2713 ' + city;
          cityHint.style.color = '#0D7377';
          cityHint.style.opacity = '1';
          cityHint.style.fontWeight = '600';
          cityHint.style.fontSize = '0.8rem';
        } else {
          cityHint.textContent = '';
          cityHint.style.color = 'transparent';
          cityHint.style.opacity = '0';
        }
      } else {
        cityHint.textContent = '';
        cityHint.style.color = 'transparent';
        cityHint.style.opacity = '0';
      }
    }

    // Initialize form from URL parameters & auto-focus ZIP
    (function() {
      var urlParams = new URLSearchParams(window.location.search);
      var urlZip = urlParams.get('zip');
      var urlAmount = urlParams.get('amount');

      if (urlZip && zipInput) {
        zipInput.value = urlZip;
        updateCityHint(urlZip);
      } else if (zipInput) {
        // Auto-focus ZIP input if no URL parameter (helps users understand they need to enter it)
        setTimeout(function() { zipInput.focus(); }, 500);
      }

      if (urlAmount && grid) {
        var targetBtn = grid.querySelector('[data-amount="' + urlAmount + '"]');
        if (targetBtn) {
          grid.querySelectorAll('.amount-btn').forEach(function(b) {
            b.classList.remove('amount-btn--active');
          });
          targetBtn.classList.add('amount-btn--active');
        }
      }
    })();

    if (grid) {
      grid.addEventListener('click', function(e) {
        var btn = e.target.closest('.amount-btn');
        if (!btn) return;
        grid.querySelectorAll('.amount-btn').forEach(function(b) {
          b.classList.remove('amount-btn--active');
        });
        btn.classList.add('amount-btn--active');
      });
    }

    if (zipInput) {
      zipInput.addEventListener('input', function() {
        clearError();
        var val = zipInput.value.replace(/\D/g, '').slice(0, 5);
        if (zipInput.value !== val) zipInput.value = val;
        updateCityHint(val);
      });
    }

    if (ctaBtn) {
      ctaBtn.addEventListener('click', function() {
        var zip = zipInput ? zipInput.value.trim() : '';
        var activeAmountBtn = grid ? grid.querySelector('.amount-btn--active') : null;
        var amount = activeAmountBtn ? activeAmountBtn.dataset.amount : '1500'; // Default: $1,500

        if (!isValidZip(zip)) {
          showError('Please enter a valid 5-digit ZIP code.');
          if (zipInput) zipInput.focus();
          return;
        }

        clearError();
        var params = new URLSearchParams({ requested_amount: amount, zip: zip });
        window.location.href = '/?' + params.toString();
      });
    }
  </script>

  <!-- Stats bar -->
  <div class="hero-stats-bar border-t border-border/50 mx-[-1.5rem] mt-8 md:mt-10">
    <div class="max-w-6xl mx-auto px-6 py-5 grid grid-cols-2 md:grid-cols-4 gap-4">
      {[
        { val: '$350M+', label: 'Transactions Financed' },
        { val: '28,000+', label: 'Partner Merchants' },
        { val: '4.8 / 5', label: 'Customer Rating' },
        { val: '60 sec', label: 'Decision Time' },
      ].map(s => (
        <div class="text-center">
          <div class="font-display font-bold text-foreground text-xl md:text-2xl mono-num">{s.val}</div>
          <div class="text-foreground/60 text-xs mt-0.5">{s.label}</div>
        </div>
      ))}
    </div>
  </div>
</section>
